
  # Library Management System Design

  This is a code bundle for Library Management System Design. The original project is available at https://www.figma.com/design/oeurBWf9bdciHc0Qd6ptDr/Library-Management-System-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  